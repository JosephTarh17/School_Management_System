import { createApp } from 'vue'
import App from './App.vue'
import './index.css'

// Flowbite JS (keep JS import here; CSS imported via Tailwind pipeline in index.css)
import 'flowbite'

createApp(App).mount('#app')
